---
alwaysApply: true
---
Các file icon sẽ lấy từ lib `lucide-react-native`

Props: 
| name |	type |	default |
| size | number |	24 |
|color	|string	|currentColor|
|strokeWidth|	number	|2|
|absoluteStrokeWidth	|boolean	|false|

Example:
```tsx
import { Camera } from 'lucide-react-native';

// Usage
const App = () => {
  return <Camera color="red" size={48} />;
};

export default App;
```